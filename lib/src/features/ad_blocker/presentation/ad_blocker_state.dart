import 'package:flutter/material.dart';

import 'ad_blocker.dart';
import 'ad_blocker_widget.dart';

class AdBlockerState extends State<AdBlocker> {
  @override
  Widget build(BuildContext context) => adBlockerWidget(context);
}
